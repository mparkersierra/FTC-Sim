plugins {
    id "application"
}

dependencies {
    implementation project(":packages:ftc-sdk-shim")
    implementation project(":vendor:TeamCode")
}

application {
    mainClass = "com.mparkersierra.ftcsim.runner.Main"
}

run {
    standardInput = System.in
}